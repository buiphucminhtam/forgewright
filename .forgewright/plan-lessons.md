# Plan Lessons — cross-session planning knowledge

> Auto-generated by ASIP Plan Quality Loop. Appended to by
> forgewright-lesson-migrator.sh --migrate-plan after each session.
>
> **Cross-Feedback (v8.3):** If a plan failure led to execution issues,
> also add a "Execution Learnings" cross-link so implementation knowledge
> informs planning.

## Session-Lessons

## 2026-05-31 — Memory Manager & Architect
### Weak Criterion: Specificity (Score: 9.5/10)
### Problem: Implementing dynamic edge weight updates and procedural nodes inside a serverless local SQLite database while ensuring foreign key constraints are strictly satisfied.
### Research Source: FluxMem paper & local SQLite schema designs.
### Lesson: When building relational graphs inside SQLite with foreign keys, always dynamically verify or initialize dependent semantic and episodic nodes (`flux_nodes`) before creating or updating their edges (`flux_edges`). Ensure Python subprocess commands in Bash subshells use proper quotes to prevent NameErrors on uninitialized variables (e.g. `summary = '$summary'`).
### Example:
  - BAD: Directly calling `graph-reinforce "current-session" "plan-quality"` which fails due to missing keys in `flux_nodes`.
  - GOOD: Running `graph-add-node "current-session"` and `graph-add-node "plan-quality"` before linking and reinforcing the edge.

<!-- Append new entries below. Format:
## [Date] — [Skill Name]
### Weak Criterion: [Criterion Name] (Score: X/1.25)
### Problem: [What the plan missed]
### Research Source: [NotebookLM notebook URL]
### Lesson: [What to always include when planning this type of work]
### Example:
  - BAD: [vague example]
  - GOOD: [specific example]
-->

## 2026-06-12 — Solution Architect
### Weak Criterion: Impact Assessment (Score: 9.0/10)
### Problem: When integrating new CI/CD workflow jobs and scripts, legacy package workflows might be left in a broken state if not fully cleaned up when their corresponding code is deleted.
### Research Source: GitHub Actions YAML and root package.json dependency structures.
### Lesson: When removing directories or modules (such as legacy `forgenexus`), always proactively review and clean up all associated CI/CD steps (e.g. `ci.yml`) and package script hooks (e.g. `package.json` scripts) to prevent remote build failures.
### Example:
  - BAD: Leaving `forgenexus-lint` and `forgenexus-test` in `.github/workflows/ci.yml` after deleting the `forgenexus/` folder.
  - GOOD: Deleting those jobs, updating the overall requirement gate (`require-all`), and redirecting the root `build`/`test`/`lint` scripts to `mcp/`.

## 2026-06-12 — Solution Architect (Migration)
### Weak Criterion: Impact Assessment (Score: 9.0/10)
### Problem: Replacing a legacy indexing system (ForgeNexus) with a new one (GitNexus) requires updating not just local scripts, but also all CI/CD workflow configuration files and custom GitHub Actions.
### Research Source: GitHub Actions workflow configurations (.github/) and gitnexus CLI help parameters.
### Lesson: When migrating core indexing/analysis packages, ensure all custom composite GitHub Actions (`action.yml`), action READMEs, and main workflows (`.yml`) are fully updated with the new CLI commands, directories (e.g. `.gitnexus/`), and environment variables (e.g. `GITNEXUS_TOKEN`).
### Example:
  - BAD: Leaving `forgenexus analyze` and `.forgenexus/` in custom composite actions after migrating locally to GitNexus.
  - GOOD: Updating all occurrences to `gitnexus analyze` and `.gitnexus/` to match the local development workspace index.

## 2026-06-15 — Solution Architect & Tech Writer
### Weak Criterion: Clarity & Reviewability (Score: 9.5/10)
### Problem: System, flow, and sequence diagrams were previously drawn using raw text art or ASCII boxes inside templates, which models duplicated, causing unreviewable diagrams in markdown documentation.
### Research Source: Mermaid.js live editor & documentation.
### Lesson: Mandate Mermaid.js block syntax (```mermaid) for all diagrams. Avoid raw ASCII/text art. Wrap participant labels containing special characters (parentheses, slashes) in double quotes `""` to prevent Mermaid syntax parser crashes.
### Example:
  - BAD: Drawing a sequence flow with text characters: `Client -> Server: request`.
  - GOOD: Writing a Mermaid sequence block:
    ```mermaid
    sequenceDiagram
        Client->>Server: HTTP request
    ```

## 2026-06-22 — Auto-Publish
### Weak Criterion: Risk Awareness (Score: 9.87/10)
### Problem: iOS App Store Connect allows programmatically creating app profiles and certificates, but Google Play Store API restricts first-time app record creation, leading to API failures if automation is triggered on a fresh Play Console account.
### Research Source: Google Play Console API & Expo EAS Submission Guidelines.
### Lesson: When designing/planning store publishing pipelines, always separate iOS and Android first-time workflows. Explicitly call out that the first-time Android app creation and initial AAB upload must be done manually via the Play Console web interface before utilizing automated submission APIs.
### Example:
  - BAD: Planning a fully automated one-click release for both platforms from a completely empty Google Play Developer account.
  - GOOD: Planning automated builds for both platforms, but alerting the user to manually create the app and upload the first AAB to the Google Play Store first.

