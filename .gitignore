# ─── Global ────────────────────────────────────────────────────────────────

# Memory / embedding caches
.forge17/
.embeddings/
.forgewright/
.gitnexus/

# Node modules (all of them)
node_modules/

# GitNexus auto-generated Claude Code files — Antigravity uses its own protocols
.claude/
CLAUDE.md

# Build output
build/
!mcp/build/
dist/
*.js.map

# ─── Project-Specific ────────────────────────────────────────────────────────

# Antigravity plugin internals (nested git submodule, caches, local MCP server)
.antigravity/.git/
.antigravity/.gitnexus/
.antigravity/.forgewright/
.antigravity/.forge17/
.antigravity/.memignore
